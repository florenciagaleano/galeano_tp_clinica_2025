import { FechaTurnoPipe } from './fecha-turno.pipe';

describe('FechaTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
