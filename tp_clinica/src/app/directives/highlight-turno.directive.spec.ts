import { HighlightTurnoDirective } from './highlight-turno.directive';

describe('HighlightTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
