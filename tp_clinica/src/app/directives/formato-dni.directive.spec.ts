import { FormatoDniDirective } from './formato-dni.directive';

describe('FormatoDniDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatoDniDirective();
    expect(directive).toBeTruthy();
  });
});
