import { AnimacionEntradaDirective } from './animacion-entrada.directive';

describe('AnimacionEntradaDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimacionEntradaDirective();
    expect(directive).toBeTruthy();
  });
});
